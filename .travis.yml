sudo: required
services:
  - docker

before_install:
  - docker build -t journald-2-cloudwatch .
install:
  - docker build -t journald-2-cloudwatch-test .
script:
  docker run --rm -v `pwd`/.coveralls:/.coveralls journald-2-cloudwatch-test

after_success:
  coveralls
